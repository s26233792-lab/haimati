# ============================================
# AI肖像馆 - 环境变量配置模板
# 复制此文件为 .env 并填入实际值
# ============================================

# ==================== API 配置 ====================
# API 提供商选择: apicore (推荐), laozhang, 12ai
API_PROVIDER=apicore

# API 密钥（从对应平台获取）
NANOBANANA_API_KEY=your-api-key-here

# 模型选择:
# - gemini-3-pro-image-preview (推荐，速度快)
# - gemini-3-pro-image-preview-2k (高分辨率)
# - gemini-2.0-flash-exp (实验版)
# - gemini-1.5-pro (稳定版)
# - gpt-4o (OpenAI)
MODEL_NAME=gemini-3-pro-image-preview

# API 基础 URL（可选，使用默认值可不填）
# API_BASE_URL=https://api.apicore.ai/v1

# ==================== Flask 应用配置 ====================
# 会话密钥（⚠️ 生产环境必须修改为随机字符串！）
# 生成随机密钥: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production

# 环境模式: development, production, testing
FLASK_ENV=production

# 调试模式（生产环境必须为 False）
FLASK_DEBUG=False

# 上传文件大小限制（字节，默认 16MB = 16 * 1024 * 1024）
MAX_CONTENT_LENGTH=16777216

# 时区设置
TZ=Asia/Shanghai

# ==================== 管理后台配置 ====================
# 管理员用户名（建议修改）
ADMIN_USERNAME=admin

# 管理员密码（⚠️ 必须修改为强密码！）
ADMIN_PASSWORD=change-this-to-strong-password

# 管理后台路径（可选，用于隐藏后台地址）
# ADMIN_PATH=admin

# ==================== 数据库配置 ====================
# 本地开发使用 SQLite
DATABASE_PATH=codes.db

# 生产环境使用 PostgreSQL（Railway 自动提供）
# DATABASE_URL=postgresql://user:password@host:port/database

# ==================== Railway 特定配置 ====================
# Railway 环境标识（自动设置）
# RAILWAY_ENVIRONMENT=production

# Railway 持久化存储路径（Volume 挂载点）
RAILWAY_VOLUME_MOUNT_PATH=/data

# ==================== 安全配置 ====================
# 允许的主机名（生产环境必须设置）
# ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# CORS 允许的来源（可选）
# CORS_ORIGINS=https://your-domain.com

# 请求限流: 每分钟最大请求数
RATE_LIMIT_PER_MINUTE=30

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 日志格式
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ==================== 缓存配置（可选） ====================
# Redis 缓存 URL（用于高并发场景）
# REDIS_URL=redis://localhost:6379/0

# 缓存过期时间（秒）
CACHE_TIMEOUT=300

# ==================== 邮件配置（可选） ====================
# 用于发送系统通知和报警
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@your-domain.com

# ==================== 监控配置（可选） ====================
# Sentry DSN（错误追踪）
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# 健康检查密钥
# HEALTH_CHECK_KEY=your-secret-health-check-key
