# 🚀 Railway部署指南

## ✅ 代码已推送到GitHub

**仓库**：https://github.com/s26233792-lab/haimati
**最新提交**：`b1c76bd` - 添加美颜开关功能

---

## 📋 自动部署检查

### 如果已配置自动部署：

Railway会自动检测到GitHub推送并触发部署，通常需要：
- ⏱️ 等待3-5分钟自动部署
- 📍 在Railway控制台查看部署状态

**检查部署状态**：
1. 登录 https://railway.app
2. 进入项目 → Deployments
3. 查看最新部署状态

### 如果未配置自动部署：

需要手动触发部署：

#### 方法1：在Railway控制台手动部署
```
1. 登录 Railway.app
2. 进入项目
3. 点击 "New Deployment" 或 "Redeploy"
4. 选择分支：main
5. 确认部署
```

#### 方法2：连接GitHub自动部署
```
1. Railway项目 → Settings
2. GitHub → Connect Repo
3. 选择：s26233792-lab/haimati
4. 启用自动部署：✅
```

---

## 🎯 部署后验证

### 1. 检查版本

访问网站，打开浏览器控制台：
```javascript
// 查看美颜开关是否存在
document.getElementById('beautifyCheckbox')
// 应该返回：HTMLElement（不是null）
```

### 2. 测试美颜功能

**步骤**：
1. 打开网站
2. 输入验证码
3. 滚动到"⑤ 美颜选项"
4. 测试开关：
   - ✅ 勾选 → 显示"轻微美颜"
   - ☐ 不勾选 → 显示"保持真实面容"
5. 上传图片并生成
6. 对比效果

### 3. 查看日志

```bash
# Railway日志
Railway → 项目 → Deployments → View Logs

# 应该看到：
[Upload] 配置: style=portrait, clothing=business_suit, ..., beautify=yes
# 或
[Upload] 配置: style=portrait, clothing=business_suit, ..., beautify=no
```

---

## 🔧 配置检查

### 环境变量确认

Railway项目 → Variables，确保已设置：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `NANOBANANA_API_KEY` | 你的API密钥 | 必须 |
| `SECRET_KEY` | 随机字符串 | 必须 |
| `PROMPT_LANGUAGE` | `zh` 或 `en` | 可选（默认zh） |
| `MODEL_NAME` | `gemini-3-pro-image-preview` | 可选 |
| `API_PROVIDER` | `apicore` | 可选 |

### 缺少配置？

如果提示缺少配置，添加方法：
```
1. Railway → 项目 → Variables
2. 点击 "New Variable"
3. 输入Name和Value
4. 保存
5. 重新部署
```

---

## ⚠️ 常见部署问题

### 问题1：部署失败

**可能原因**：
- 依赖安装失败
- 环境变量缺失
- 数据库连接问题

**解决方案**：
```bash
# 查看部署日志
Railway → Deployments → View Logs

# 检查错误信息，通常包含：
- Error: Module not found
- Error: API_KEY not configured
- Error: Database connection failed
```

### 问题2：部署成功但功能异常

**检查清单**：
- ✅ 环境变量是否设置
- ✅ API Key是否有效
- ✅ 数据库是否初始化
- ✅ 验证码是否生成

**调试步骤**：
```bash
# 访问调试端点
https://your-app.railway.app/debug/config

# 返回示例：
{
  "api_key_configured": true,
  "api_key_length": 32,
  "db_type": "PostgreSQL",
  "upload_folder_exists": true
}
```

### 问题3：新功能未生效

**可能原因**：
- 缓存问题
- 旧版本仍在运行
- 部署未完成

**解决方案**：
```
1. 清除浏览器缓存（Ctrl+Shift+Delete）
2. 硬刷新（Ctrl+F5）
3. 检查Railway部署状态
4. 等待部署完成（3-5分钟）
```

---

## 📊 部署状态监控

### 实时日志

```bash
Railway → 项目 → Deployments → View Logs

# 关键日志：
[API] 使用模型: gemini-3-pro-image-preview
[API] API Key 已配置
[DB] 数据库初始化成功
✅ API肖像馆启动成功
```

### 健康检查

访问健康检查端点：
```
https://your-app.railway.app/debug/health
```

应该返回：
```json
{
  "status": "healthy",
  "checks": {
    "database": {"status": "ok"},
    "api_key": {"status": "configured"},
    "upload_folder": {"status": "ok"}
  }
}
```

---

## 🎉 部署完成后

### 功能验证清单

- [ ] 网站可以正常访问
- [ ] 验证码功能正常
- [ ] 图片上传功能正常
- [ ] **美颜开关显示** ⭐
- [ ] 美颜功能正常工作 ⭐
- [ ] 生成图片成功
- [ ] 验证码次数正确扣减

### 分享链接

部署成功后，分享你的应用：
```
https://your-app.railway.app
```

---

## 📞 需要帮助？

### Railway支持
- Discord: https://discord.gg/railway
- Email: support@railway.app
- Docs: https://docs.railway.app

### 查看部署日志
```bash
Railway控制台 → 项目 → Deployments → View Logs
```

---

*部署指南更新时间：2026-02-07*
*GitHub提交：b1c76bd*
